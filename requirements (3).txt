# ─────────────────────────────────────────────
# Stream Minds — Python Dependencies
# Install: pip install -r requirements.txt
# Python: 3.10+
# ─────────────────────────────────────────────

# ── Web Framework ──
Flask==3.0.3
flask-cors==4.0.1

# ── Database ──
Flask-SQLAlchemy==3.1.1
psycopg2-binary==2.9.9        # PostgreSQL driver
SQLAlchemy==2.0.30

# ── Authentication ──
Flask-JWT-Extended==4.6.0
Werkzeug==3.0.3               # password hashing

# ── Data Science / ML ──
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2           # cosine similarity, TF-IDF

# ── NLP (explainability) ──
spacy==3.7.4
# After install, run: python -m spacy download en_core_web_sm

# ── Environment & Config ──
python-dotenv==1.0.1

# ── Dev / Testing ──
pytest==8.2.0
pytest-flask==1.3.0

# ─────────────────────────────────────────────
# OPTIONAL: uncomment for production
# ─────────────────────────────────────────────
# gunicorn==22.0.0            # production WSGI server
# sagemaker==2.221.0          # AWS ML hosting
# boto3==1.34.131             # AWS SDK
